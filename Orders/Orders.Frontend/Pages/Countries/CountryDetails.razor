@page "/countries/details/{CountryId:int}"

<h3>Detalle País</h3>

@if (country is null)
{
    <Loading />
}
else
{
    <h3>@country.Name</h3>

    <a class="btn btn-primary" href="/states/create/@country.Id">Nuevo Estado/Departamento</a>
    <a class="btn btn-success" href="/countries">Regresar</a>

    <h4>Estados/Departamentos</h4>
    @* <GenericList MyList="country.States!.ToList()"> *@
    <GenericList MyList="states!">
        <Body>

            <Pagination CurrentPage="currentPage"
                        TotalPages="totalPages"
                        SelectedPage="SelectedPageAsync" />

            <div class="mb-2" style="display: flex; flex-wrap:wrap; align-items: center;">
                <input style="width: 400px;" type="text" class="form-control" id="titulo" placeholder="Buscar país..." @bind-value="Filter" />
                <botton type="button" class="btn btn-outline-primary mx-1" @onclick="ApplyFilterAsync">Filtrar</botton>
                <botton type="button" class="btn btn-outline-danger" @onclick="CleanFilterAsync">Limpiar</botton>
            </div>

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th style="width:90px">Estado / Departamento</th>
                        <th style="width:90px">Ciudades</th>
                        <th style="width:168px"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var state in states!)
                    {
                        <tr>
                            <td><a href="/states/details/@state.Id">@state.Name</a></td>
                            <td>@state.CitiesNumber</td>
                            <td>
                                @* <a class="btn btn-warning btn-sm" @onclick=@(() => ShowModalAsync(state.Id, true))><i class="bi bi-pencil" /> Editar</a> *@
                                <a class="btn btn-warning btn-sm" href="/states/edit/@state.Id"><i class="bi bi-pencil" /> Editar</a>
                                <a class="btn btn-warning btn-sm" href="/states/details/@state.Id"><i class="bi bi-pencil" /> Detalles</a>
                                <button class="btn btn-danger btn-sm" @onclick=@(() => DeleteAsync(state))><i class="bi bi-trash" /> Borrar</button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </Body>
    </GenericList>
}

@*   <h3>@country.Name</h3>
    <div class="mb-2">
        <a class="btn btn-primary" href="/states/create/@country.Id">Nuevo Estado/Departamento</a>
        <a class="btn btn-success" href="/countries">Regresar</a>
     <div />

    <h4>Estados/Departamentos</h4>
      <GenericList MyList="country.States!.ToList()">
                <Body>

                     <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Estado / Departamento</th>
                                <th style="width:90px">Ciudades</th>
                                <th style="width:168px"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var state in country.States!)
                            {
                                <tr>
                                    <td><a href="/states/details/@state.Id">@state.Name</a></td>
                                    <td>@state.CitiesNumber</td>
                                    <td>
                                      <a class="btn btn-warning btn-sm" @onclick=@(() => ShowModalAsync(state.Id, true))><i class="bi bi-pencil" /> Editar</a> 
                                         <a class="btn btn-warning btn-sm" href="/states/details/@state.Id"><i class="bi bi-pencil" /> Editar</a> 
                                        <a class="btn btn-warning btn-sm" href="/states/details/@state.Id"><i class="bi bi-pencil" /> Detalles</a> 
                                       <button class="btn btn-danger btn-sm" @onclick=@(() => DeleteAsync(state))><i class="bi bi-trash" /> Borrar</button> 
                                         <button class="btn btn-danger btn-sm" @onclick=@(() => DeleteAsync(state))><i class="bi bi-trash" /> Borrar</button> 
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                 </Body> 
            </GenericList> *@